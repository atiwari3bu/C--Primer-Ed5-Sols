/* To calculate rate we can use float as we do not require 
 * much high value with precision.
 * For principal, we can use double as principal might require
 * huge capital with precision.
 * For payment, we can use use float as payement is subsidiary 
 * of principal and usually less than that.
*/


int main(){
  return 0;
}
