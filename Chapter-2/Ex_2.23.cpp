#include<iostream>

int main(){

  // I cannot determine whether p points to valid object or not just by looking
  // Only the programmer who have assigned the value to p can detemine whether
  // the points to valid object by dereferencing and checking.
  //
  return 0;

}
