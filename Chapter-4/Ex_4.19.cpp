#include<iostream>
using namespace std;

int main(){

  prtr!=0 && *ptr++; // Incorrect method
  *ptr!=0 && *ptr++; // Correct method

  ival++ && ival; // incorrect method
  ival && ival++; // correct method

  vec[ival++]<=vec[ival]; // incorrect method
  vev[ival++] < = (vec[ival]); // correct method

  return 0;
}



