Prefix operators does not create the copy of object, performs the operation and
return that object.

Postfix operators create the copy of object, performs the operation and return 
the copy of that object.
