#include<iostream>

using namespace std;

int main(){

  const char* cp = "Hello World";
  if(cp && *cp);  // if cp i.e Hello World and value
  // pointer by Hello World address is not zero then
  // this returns true otherwise false

  return 0;
}
