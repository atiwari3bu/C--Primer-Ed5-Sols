#include<iostream>

int main(){
  std::cout<<"(1) If they are equal then it will print the number of times the given input has occured"<<std::endl;
  std::cout<<"(2) If there are no duplicate values then it will print the numbers with number of occurences which will be 1 everytime"<<std::endl;
  return 0;
}
